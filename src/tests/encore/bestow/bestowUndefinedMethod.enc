passive class IntBox
   var bar : int

   def init(bar : int) : unit
      this.setInt(bar)
   end
end

class Foo
   var box : IntBox

   def init(bar : int) : unit
      this.box = new IntBox(bar)
   end

   def bestowBox() : Bestow[IntBox]
      bestow(this.box)
   end
end

class Main
   def main() : unit
      var b = new Foo(7)
      var box = get(b ! bestowBox())
      box ! foo()
   end
end
