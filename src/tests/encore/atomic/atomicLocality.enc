active class Foo : Actor
end

local class Bar
  var i : int

  def dummy() : Bar
    new Bar()
  end
end

active class Main
  def main() : unit
    var bar = new Bar()
    bar.i = 0
    var x = 0
    atomic new Foo() as foo in
      var y = x
      println(y)
      y += 1
    end
    println(x)
  end
end
